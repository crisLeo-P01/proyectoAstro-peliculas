---
interface Props {
  pregunta: string,
  respuesta: string,
}

const { pregunta, respuesta } = Astro.props;
---

<div class="flex flex-col gap-9 bg-[#231A31] mb-5 rounded-lg">
  <p>{pregunta}</p>
  <span>{respuesta}</span>
</div>

